# Broken pomodoro
## About
- attempt to build `like-pomodoro` with my own version but i end up building `regular pomodoro`, i used `egui` crate to build it
- it support both `Linux` and `Windows` (not tested on macos)
## Things that i learned
- before you begin your projects, structure it 
- Make it work first, then make it clean or redesign it (i could be wrong here), but i know it always correct for optimization
